import '../../../../app.dart';

class DashboardScreen extends StatelessWidget {
  const DashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // return Scaffold(body: Center(child: CustomText(text: "Dashboard")));
    return AttendanceScreen();
  }
}
